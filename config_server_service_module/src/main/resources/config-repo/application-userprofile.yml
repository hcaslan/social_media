server:
  port: 9091
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: UserProfileDB
      username: bilge
      password: bilge!123
userprofileservice:
  secret:
    secret-key: ${JAVA14_SECRET_KEY}
    issuer: ${JAVA14_ISSUER}